#!/usr/bin/env zsh

if ! command -v op >/dev/null 2>&1; then
    print -u2 "1Password CLI is required: op not found"
    return 1 2>/dev/null || exit 1
fi

export ENGRAM_CLOUD_TOKEN="$(op read 'op://Secrets/Engram Cloud/password')"

print "Loaded secrets: ENGRAM_CLOUD_TOKEN"
