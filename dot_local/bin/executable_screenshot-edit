#!/usr/bin/env bash
set -euo pipefail

geometry="$(slurp -o -r -c '#cba6f7ff')"

grim -g "${geometry}" -t ppm - \
  | satty --filename - \
    --fullscreen \
    --copy-command wl-copy \
    --actions-on-enter save-to-clipboard \
    --actions-on-escape exit
