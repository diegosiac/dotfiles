# Local/private files
.env
.env.*
*.local
*.secret
*.secrets

# Chezmoi local state must never be committed
.chezmoidata.*
.chezmoitemplates/*secret*

# Editor and OS noise
.DS_Store
*.swp
*.swo
*~

# Agent tooling metadata (generated per-user, not product code)
.atl/
.pi

# Engram local runtime state (project sync chunks are intentionally versioned)
.engram/engram.db
.engram/cloud.json
